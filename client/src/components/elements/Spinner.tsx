const Spinner = () => {
  return (
    <span className="inline-block h-5 w-5 animate-spin rounded-full border-2 border-solid border-slate-600 border-b-transparent" />
  );
};

export default Spinner;
